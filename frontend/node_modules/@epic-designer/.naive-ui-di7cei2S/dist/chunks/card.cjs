'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const card = vue.defineComponent({
  props: {
    componentSchema: {
      default: () => ({}),
      require: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a;
      const componentSchema = {
        ...props.componentSchema,
        title: props.componentSchema.label
      };
      const children = (_a = componentSchema.children) != null ? _a : [];
      delete componentSchema.children;
      return vue.h(naiveUi.NCard, componentSchema, {
        default: () => vue.renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (node) => vue.renderSlot(slots, "node", { componentSchema: node })
          )
        )
      });
    };
  }
});

exports.default = card;
