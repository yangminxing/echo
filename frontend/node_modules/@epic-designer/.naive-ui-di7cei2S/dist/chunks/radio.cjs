'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const radio = vue.defineComponent({
  emits: ["update:modelValue"],
  setup(_, { attrs, emit }) {
    function handleUpdate(e = null) {
      emit("update:modelValue", e);
    }
    return () => {
      const props = {
        ...attrs,
        "onUpdate:value": handleUpdate,
        value: attrs.modelValue
      };
      return vue.h(naiveUi.NRadioGroup, props, {
        default: () => {
          var _a;
          return [
            (_a = props.options) == null ? void 0 : _a.map(
              (option) => vue.h(
                naiveUi.NRadio,
                { key: option.value, value: option.value },
                { default: () => option.label }
              )
            )
          ];
        }
      });
    };
  }
});

exports.default = radio;
