'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const tabPane = vue.defineComponent({
  name: "TabPane",
  props: {
    componentSchema: {
      default: () => ({}),
      require: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a, _b;
      const componentSchema = {
        ...props.componentSchema,
        name: props.componentSchema.label
      };
      const children = (_b = (_a = props.componentSchema) == null ? void 0 : _a.children) != null ? _b : [];
      delete componentSchema.children;
      delete componentSchema.label;
      return vue.h(naiveUi.NTabPane, componentSchema, {
        default: () => vue.renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (nodeProps) => vue.renderSlot(slots, "node", { componentSchema: nodeProps })
          )
        )
      });
    };
  }
});

exports.default = tabPane;
