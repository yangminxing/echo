import { defineComponent, h, renderSlot } from 'vue';
import { NTabPane } from 'naive-ui';

const tabPane = defineComponent({
  name: "TabPane",
  props: {
    componentSchema: {
      default: () => ({}),
      require: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a, _b;
      const componentSchema = {
        ...props.componentSchema,
        name: props.componentSchema.label
      };
      const children = (_b = (_a = props.componentSchema) == null ? void 0 : _a.children) != null ? _b : [];
      delete componentSchema.children;
      delete componentSchema.label;
      return h(NTabPane, componentSchema, {
        default: () => renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (nodeProps) => renderSlot(slots, "node", { componentSchema: nodeProps })
          )
        )
      });
    };
  }
});

export { tabPane as default };
