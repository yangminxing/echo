'use strict';

const vue = require('vue');
const elementPlus = require('element-plus');
require('element-plus/es/components/select/style/css');

const select = vue.defineComponent({
  emits: ["update:modelValue"],
  setup(_, { attrs, emit }) {
    function handleUpdate(e = null) {
      emit("update:modelValue", e);
    }
    return () => {
      const props = {
        ...attrs,
        key: String(attrs.multiple),
        "onUpdate:modelValue": handleUpdate
      };
      return vue.h(elementPlus.ElSelect, props, {
        default: () => {
          var _a;
          return [
            (_a = props.options) == null ? void 0 : _a.map(
              (option) => vue.h(elementPlus.ElOption, { label: option.label, value: option.value })
            )
          ];
        }
      });
    };
  }
});

exports.default = select;
