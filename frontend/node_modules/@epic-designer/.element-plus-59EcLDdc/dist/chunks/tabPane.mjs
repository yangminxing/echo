import { defineComponent, computed, createBlock, openBlock, unref, normalizeProps, guardReactiveProps, withCtx, renderSlot, createElementBlock, Fragment, renderList } from 'vue';
import { ElTabPane } from 'element-plus';

const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "tabPane",
  props: {
    componentSchema: { type: Object, required: true }
  },
  setup(__props) {
    const props = __props;
    const children = computed(() => {
      var _a;
      return ((_a = props.componentSchema) == null ? void 0 : _a.children) || [];
    });
    const componentProps = computed(() => {
      var _a, _b;
      const schema = { ...props.componentSchema };
      delete schema.children;
      return {
        ...schema,
        ...(_a = props.componentSchema) == null ? void 0 : _a.props,
        name: (_b = props.componentSchema) == null ? void 0 : _b.label
      };
    });
    return (_ctx, _cache) => {
      return openBlock(), createBlock(
        unref(ElTabPane),
        normalizeProps(guardReactiveProps(componentProps.value)),
        {
          default: withCtx(() => [
            renderSlot(_ctx.$slots, "edit-node", {}, () => [
              (openBlock(true), createElementBlock(
                Fragment,
                null,
                renderList(children.value, (child) => {
                  return renderSlot(_ctx.$slots, "node", {
                    key: child.id,
                    componentSchema: child
                  });
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ]),
          _: 3
          /* FORWARDED */
        },
        16
        /* FULL_PROPS */
      );
    };
  }
});

export { _sfc_main as default };
