'use strict';

const vue = require('vue');
const elementPlus = require('element-plus');

const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "tabPane",
  props: {
    componentSchema: { type: Object, required: true }
  },
  setup(__props) {
    const props = __props;
    const children = vue.computed(() => {
      var _a;
      return ((_a = props.componentSchema) == null ? void 0 : _a.children) || [];
    });
    const componentProps = vue.computed(() => {
      var _a, _b;
      const schema = { ...props.componentSchema };
      delete schema.children;
      return {
        ...schema,
        ...(_a = props.componentSchema) == null ? void 0 : _a.props,
        name: (_b = props.componentSchema) == null ? void 0 : _b.label
      };
    });
    return (_ctx, _cache) => {
      return vue.openBlock(), vue.createBlock(
        vue.unref(elementPlus.ElTabPane),
        vue.normalizeProps(vue.guardReactiveProps(componentProps.value)),
        {
          default: vue.withCtx(() => [
            vue.renderSlot(_ctx.$slots, "edit-node", {}, () => [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList(children.value, (child) => {
                  return vue.renderSlot(_ctx.$slots, "node", {
                    key: child.id,
                    componentSchema: child
                  });
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ]),
          _: 3
          /* FORWARDED */
        },
        16
        /* FULL_PROPS */
      );
    };
  }
});

exports.default = _sfc_main;
