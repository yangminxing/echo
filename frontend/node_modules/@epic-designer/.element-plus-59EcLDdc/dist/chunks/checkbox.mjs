import { defineComponent, h } from 'vue';
import { ElCheckboxGroup, ElCheckboxButton, ElCheckbox } from 'element-plus';
import 'element-plus/es/components/select/style/css';

const checkbox = defineComponent({
  emits: ["update:modelValue"],
  setup(_, { attrs, emit }) {
    function handleUpdate(e = null) {
      emit("update:modelValue", e);
    }
    return () => {
      const props = {
        ...attrs,
        "onUpdate:modelValue": handleUpdate
      };
      return h(ElCheckboxGroup, props, {
        default: () => {
          var _a, _b;
          return [
            (props == null ? void 0 : props.radioButton) ? (_a = props.options) == null ? void 0 : _a.map(
              (option) => h(ElCheckboxButton, {
                label: option.label,
                value: option.value
              })
            ) : (_b = props.options) == null ? void 0 : _b.map(
              (option) => h(ElCheckbox, { label: option.label, value: option.value })
            )
          ];
        }
      });
    };
  }
});

export { checkbox as default };
