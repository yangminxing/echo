'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const button = vue.defineComponent({
  props: {
    componentSchema: {
      default: () => ({}),
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a;
      const componentProps = {
        ...(_a = props.componentSchema) == null ? void 0 : _a.props
      };
      return vue.h(naiveUi.NButton, componentProps, {
        default: () => vue.renderSlot(slots, "default", {}, () => {
          var _a2;
          return [
            (_a2 = props.componentSchema) == null ? void 0 : _a2.label
          ];
        })
      });
    };
  }
});

exports.default = button;
