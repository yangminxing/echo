import { defineComponent, h, renderSlot } from 'vue';
import { NCollapseItem } from 'naive-ui';

const collapseItem = defineComponent({
  props: {
    componentSchema: {
      default: () => ({}),
      require: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a, _b;
      const componentSchema = {
        ...props.componentSchema,
        title: (_a = props.componentSchema.label) != null ? _a : ""
      };
      const children = (_b = componentSchema.children) != null ? _b : [];
      delete componentSchema.children;
      return h(NCollapseItem, componentSchema, {
        default: () => renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (node) => renderSlot(slots, "node", { componentSchema: node })
          )
        )
      });
    };
  }
});

export { collapseItem as default };
