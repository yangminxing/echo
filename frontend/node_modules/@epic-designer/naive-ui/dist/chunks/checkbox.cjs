'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const checkbox = vue.defineComponent({
  emits: ["update:modelValue"],
  setup(_, { attrs, emit }) {
    function handleUpdate(e = null) {
      emit("update:modelValue", e);
    }
    return () => {
      const props = {
        ...attrs,
        "onUpdate:value": handleUpdate,
        value: attrs.modelValue
      };
      return vue.h(naiveUi.NCheckboxGroup, props, {
        default: () => {
          var _a;
          return [
            (_a = props.options) == null ? void 0 : _a.map(
              (option) => vue.h(naiveUi.NCheckbox, { label: option.label, value: option.value })
            )
          ];
        }
      });
    };
  }
});

exports.default = checkbox;
