'use strict';

const vue = require('vue');
const naiveUi = require('naive-ui');

const collapseItem = vue.defineComponent({
  props: {
    componentSchema: {
      default: () => ({}),
      require: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a, _b;
      const componentSchema = {
        ...props.componentSchema,
        title: (_a = props.componentSchema.label) != null ? _a : ""
      };
      const children = (_b = componentSchema.children) != null ? _b : [];
      delete componentSchema.children;
      return vue.h(naiveUi.NCollapseItem, componentSchema, {
        default: () => vue.renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (node) => vue.renderSlot(slots, "node", { componentSchema: node })
          )
        )
      });
    };
  }
});

exports.default = collapseItem;
