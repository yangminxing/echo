import { PluginManager, SetupConfig } from 'epic-designer';

function styleInject(css, ref) {
  if ( ref === void 0 ) ref = {};
  var insertAt = ref.insertAt;

  if (typeof document === 'undefined') { return; }

  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';

  if (insertAt === 'top') {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }

  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}

var css_248z = ".n-collapse .n-collapse-item:first-child > .n-collapse-item__header,\n.n-collapse .n-collapse-item > .n-collapse-item__header {\n  padding-top: 16px;\n}\n.n-collapse .n-collapse-item:first-child > .n-collapse-item__header .n-collapse-item__header-main,\n.n-collapse .n-collapse-item > .n-collapse-item__header .n-collapse-item__header-main {\n  padding-left: 8px;\n}\n.ep-input-size > .n-input-wrapper {\n  padding-right: 0;\n}\n.ep-input-size .n-base-selection-input {\n  padding: 0 4px !important;\n}\n.ep-input-size .n-base-selection__state-border,\n.ep-input-size .n-base-selection__border {\n  border: 0 !important;\n  box-shadow: none !important;\n}\n.epic-modal-n {\n  margin: 0 auto 0 !important;\n  height: calc(100vh - 50px);\n}\n.epic-modal-n .n-card-header {\n  padding: 16px 16px 0;\n}\n.epic-modal-n .n-card__content {\n  padding: 0;\n  height: calc(100% - 56px);\n}\n.epic-modal-n .n-card__content .epic-modal-main {\n  height: calc(100% - 56px);\n  overflow: auto;\n  padding: 12px;\n}\n";
styleInject(css_248z);

declare function setupNaiveUi(pluginManager?: PluginManager, config?: SetupConfig): void;

export { setupNaiveUi };
