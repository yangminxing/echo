'use strict';

const vue = require('vue');
const elementPlus = require('element-plus');
require('element-plus/es/components/select/style/css');

const checkbox = vue.defineComponent({
  emits: ["update:modelValue"],
  setup(_, { attrs, emit }) {
    function handleUpdate(e = null) {
      emit("update:modelValue", e);
    }
    return () => {
      const props = {
        ...attrs,
        "onUpdate:modelValue": handleUpdate
      };
      return vue.h(elementPlus.ElCheckboxGroup, props, {
        default: () => {
          var _a, _b;
          return [
            (props == null ? void 0 : props.radioButton) ? (_a = props.options) == null ? void 0 : _a.map(
              (option) => vue.h(elementPlus.ElCheckboxButton, {
                label: option.label,
                value: option.value
              })
            ) : (_b = props.options) == null ? void 0 : _b.map(
              (option) => vue.h(elementPlus.ElCheckbox, { label: option.label, value: option.value })
            )
          ];
        }
      });
    };
  }
});

exports.default = checkbox;
