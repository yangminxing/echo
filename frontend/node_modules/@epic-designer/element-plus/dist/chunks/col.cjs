'use strict';

const vue = require('vue');
const elementPlus = require('element-plus');

const col = vue.defineComponent({
  props: {
    componentSchema: {
      default: () => ({}),
      required: true,
      type: Object
    }
  },
  setup(props, { slots }) {
    return () => {
      var _a, _b;
      const componentSchema = {
        ...props.componentSchema,
        title: (_a = props.componentSchema) == null ? void 0 : _a.label
      };
      const children = (_b = componentSchema.children) != null ? _b : [];
      delete componentSchema.children;
      return vue.h(elementPlus.ElCol, componentSchema, {
        default: () => vue.renderSlot(
          slots,
          "edit-node",
          {},
          () => children.map(
            (node) => vue.renderSlot(slots, "node", { componentSchema: node })
          )
        )
      });
    };
  }
});

exports.default = col;
