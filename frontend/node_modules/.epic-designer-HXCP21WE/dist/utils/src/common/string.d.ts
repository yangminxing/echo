/**
 * 生成一个用不重复的ID
 * @param randomLength 随机id长度 0 - 11
 */
export declare function getUUID(randomLength?: number, type?: 'number' | 'string'): string;
/**
  将字符串的首字母大写
  @param str 待处理字符串
  @returns string 首字母大写后的字符串
  */
export declare function capitalizeFirstLetter(str: string): string;
/**
 * 从 URL 中提取文件名
 * @param url - 包含文件名的 URL
 * @returns 提取的文件名，如果没有则返回空字符串
 */
export declare function getFileNameByUrl(url: string): string;
