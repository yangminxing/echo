import { inject as a, provide as n } from "vue";
const r = Symbol("pageManager");
function g(e) {
  n(r, e);
}
function t() {
  const e = a(r);
  if (!e)
    throw new Error("未获取到 pageManager 实例");
  return e;
}
export {
  r as PAGE_MANAGER_KEY,
  g as providePageManager,
  t as usePageManager
};
