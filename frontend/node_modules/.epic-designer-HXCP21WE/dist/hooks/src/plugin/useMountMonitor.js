import { ref as r, computed as p } from "vue";
function c() {
  const n = r([]), t = p(() => n.value.length === 0);
  function u(e) {
    n.value.push(e);
  }
  function o(e) {
    n.value = n.value.filter((l) => l !== e);
  }
  function i() {
    n.value = [];
  }
  return {
    isAllMounted: t,
    pendingIds: n,
    pop: o,
    push: u,
    reset: i
  };
}
export {
  c as useMountMonitor
};
