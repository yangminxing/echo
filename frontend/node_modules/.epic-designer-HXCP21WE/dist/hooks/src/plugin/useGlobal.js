import { reactive as a } from "vue";
function l(o) {
  const e = (n) => {
    console.warn(`[Epic]全局提示函数未注册 提示信息：'${n}'`);
  };
  return {
    $message: {
      error: e,
      info: e,
      success: e,
      warning: e
    },
    ...o
  };
}
function t(o) {
  const e = l(o);
  return {
    global: a(e)
  };
}
export {
  t as useGlobal
};
