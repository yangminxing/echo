import { Revoke } from '../../../../../manager/src/index';
import { DesignerState, PageSchema } from '../../../../../types/src/index';
interface DesignerHotkeysDeps {
    emit: (event: string, ...args: any[]) => void;
    handleDelete: () => false | undefined;
    pageSchema: PageSchema;
    revoke: Revoke;
    setSelectedNode: (node: any) => void;
    state: DesignerState;
}
export declare function useHotkeys(deps: DesignerHotkeysDeps): {
    cleanup: () => void;
    setTarget: (newTarget: Document | HTMLElement) => void;
};
export {};
