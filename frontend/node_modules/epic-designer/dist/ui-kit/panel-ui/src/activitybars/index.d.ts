import { PluginManager } from '../../../../manager/src/index';
/**
 * 初始化活动栏，向插件管理器注册活动栏项。
 *
 * @param {PluginManager} pluginManager - 插件管理器实例，用于注册活动栏项。
 * @returns {void}
 */
export declare function setupActivitybar(pluginManager: PluginManager): void;
