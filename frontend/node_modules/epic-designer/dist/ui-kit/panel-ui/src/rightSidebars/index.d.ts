import { PluginManager } from '../../../../manager/src/index';
/**
 * 初始化右侧边栏，向插件管理器注册右侧边栏项。
 *
 * @param {PluginManager} pluginManager - 插件管理器实例，用于注册右侧边栏项。
 * @returns {void}
 */
export declare function setupRightSidebar(pluginManager: PluginManager): void;
