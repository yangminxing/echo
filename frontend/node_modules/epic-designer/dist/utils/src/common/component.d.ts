import { AsyncComponentLoader, Component } from 'vue';
/**
 * 异步加载组件
 * @param loader
 */
export declare const loadAsyncComponent: (loader: AsyncComponentLoader, loadingComponent?: Component) => any;
